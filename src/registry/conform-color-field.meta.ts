import type { ComponentMeta } from "./types"

export const conformColorFieldMeta: ComponentMeta = {
  slug: "conform-color-field",
  name: "Conform Color Field",
  description:
    "The quebi ColorField wired to Conform. Binds a hex-string field's name, required, default, and validity from field metadata and renders inline errors.",
  category: "Forms",
  tags: ["form", "conform", "color", "hex", "validation"],
}
