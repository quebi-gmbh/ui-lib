import type { ComponentMeta } from "./types"

export const stepperMeta: ComponentMeta = {
  slug: "stepper",
  name: "Stepper",
  description:
    "Horizontal progress indicator for multi-step flows, with active and completed steps highlighted in quebi brand teal — flat by default, with an opt-in `glow` halo for kiosk and hero surfaces. Admin (labelled) and kiosk (bullet-only) variants.",
  category: "Navigation",
  tags: ["navigation", "progress", "wizard", "steps", "stepper"],
}
