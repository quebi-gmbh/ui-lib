import type { ComponentMeta } from "./types"

export const itemMeta: ComponentMeta = {
  slug: "item",
  name: "Item",
  description:
    "A static list row — leading media (Avatar or IconTile), a title with a secondary line, trailing meta and a trailing action — stacked by ItemGroup with a hairline between rows. The replacement for a card per list item; rows the user navigates or selects are a GridList or ListBox instead.",
  category: "Display",
  tags: ["display", "list", "row", "list-item", "media", "avatar"],
}
