import type { ComponentMeta } from "./types"

export const conformWeekPickerMeta: ComponentMeta = {
  slug: "conform-week-picker",
  name: "Conform Week Picker",
  description:
    "The quebi Week Picker wired to Conform. Submits the whole week as two ISO dates, name.start and name.end, through a hidden fieldset — never an ambiguous YYYY-Www token.",
  category: "Conform",
  tags: ["form", "conform", "week", "date", "range", "picker", "validation"],
}
