import type { ComponentMeta } from "./types"

export const colorSwatchMeta: ComponentMeta = {
  slug: "color-swatch",
  name: "Color Swatch",
  description:
    "Accessible color swatch built on react-aria-components, styled with the quebi design system. Renders a square filled with a color value, with a subtle inset hairline; one size-* class from the caller overrides it at every breakpoint.",
  category: "Color",
  tags: ["form", "color", "swatch", "display"],
}
