import type { ComponentMeta } from "./types"

export const calendarShellMeta: ComponentMeta = {
  slug: "calendar-shell",
  name: "Calendar Shell",
  description:
    "The time grid every Outlook-style view is drawn from: hour axis and gridlines, an all-day band that folds into \"+N more\", a now-marker, and overlapping events packed into side-by-side columns. Day View, Week View and Month View are assemblies over it.",
  category: "Date & time",
  tags: ["calendar", "events", "schedule", "grid", "agenda", "overlap", "shell"],
}
