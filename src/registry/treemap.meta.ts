import type { ComponentMeta } from "./types"

export const treemapMeta: ComponentMeta = {
  slug: "treemap",
  name: "Treemap",
  description:
    "Hierarchical data as nested rectangles on the quebi Chart wrapper: every top-level branch takes one palette hue and its descendants inherit it. Requires the recharts package.",
  category: "Charts",
  tags: ["chart", "treemap", "hierarchy", "nested", "recharts", "data", "visualization"],
}
