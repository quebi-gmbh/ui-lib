import type { ComponentMeta } from "./types"

export const tableMeta: ComponentMeta = {
  slug: "table",
  name: "Table",
  description:
    "Accessible data table built on react-aria-components, styled with the quebi design system. Supports row selection, sorting, drag-and-drop, resizable columns, striping, and grid lines. Draws its own bordered panel by default; the plain variant draws none, for a table under a heading or inside a Card, and bleed lines its outer columns up with the text around it.",
  category: "Display",
  tags: ["table", "data", "grid", "list", "selection", "sorting"],
}
