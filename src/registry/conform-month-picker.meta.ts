import type { ComponentMeta } from "./types"

export const conformMonthPickerMeta: ComponentMeta = {
  slug: "conform-month-picker",
  name: "Conform Month Picker",
  description:
    "The quebi Month Picker wired to Conform. Binds a date field through a hidden input carrying an ISO YYYY-MM-DD string — the first of the chosen month — rather than a bespoke YYYY-MM token.",
  category: "Conform",
  tags: ["form", "conform", "month", "date", "picker", "validation"],
}
