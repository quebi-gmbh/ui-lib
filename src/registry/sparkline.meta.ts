import type { ComponentMeta } from "./types"

export const sparklineMeta: ComponentMeta = {
  slug: "sparkline",
  name: "Sparkline",
  description:
    "An inline, text-sized trend glyph in one SVG element — line, area or bars, with no axes, no tooltip and no recharts, because it goes in a table cell hundreds at a time. Absolute by default, so two rows can be compared.",
  category: "Charts",
  tags: ["sparkline", "trend", "inline", "svg", "chart", "table"],
}
