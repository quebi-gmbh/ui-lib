import type { ComponentMeta } from "./types"

export const signalBarsMeta: ComponentMeta = {
  slug: "signal-bars",
  name: "Signal Bars",
  description:
    "A discrete level on an absolute scale — connection quality, confidence, load — drawn in four or five steps with the unfilled ones left in place. Shares its geometry with Activity Pulse so the pair reads as the question that chooses between them: a level, or a rhythm?",
  category: "Feedback",
  tags: ["level", "signal", "strength", "confidence", "indicator", "feedback"],
}
