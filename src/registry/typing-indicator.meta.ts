import type { ComponentMeta } from "./types"

export const typingIndicatorMeta: ComponentMeta = {
  slug: "typing-indicator",
  name: "Typing Indicator",
  description:
    "Three staggered dots that mean a reply is being composed — a narrower claim than \"a process is running\", and the one indicator in the family with no data feed behind it. Pure CSS, and three static dots under prefers-reduced-motion.",
  category: "Feedback",
  tags: ["typing", "chat", "composing", "dots", "loading", "feedback"],
}
