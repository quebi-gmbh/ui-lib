import type { ComponentMeta } from "./types"

export const radarChartMeta: ComponentMeta = {
  slug: "radar-chart",
  name: "Radar Chart",
  description:
    "A polar radar (spider) chart on the quebi Chart wrapper, overlaying one themed polygon per config key across a shared set of axes. Requires the recharts package.",
  category: "Charts",
  tags: ["chart", "radar", "spider", "polar", "recharts", "data", "visualization"],
}
