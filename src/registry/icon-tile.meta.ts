import type { ComponentMeta } from "./types"

export const iconTileMeta: ComponentMeta = {
  slug: "icon-tile",
  name: "IconTile",
  description:
    "Non-interactive tinted box holding a single icon — the leading glyph of a feature row, empty state or stat card. Badge's intents on Button's square size scale, so a tile lines up with the icon button beside it.",
  category: "Display",
  tags: ["icon", "tile", "badge", "decorative", "display"],
}
