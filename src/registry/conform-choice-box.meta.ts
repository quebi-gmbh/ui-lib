import type { ComponentMeta } from "./types"

export const conformChoiceBoxMeta: ComponentMeta = {
  slug: "conform-choice-box",
  name: "Conform Choice Box",
  description:
    "The quebi ChoiceBox wired to Conform. Binds a card-style selection through a hidden select, so a GridList with no native form value submits its keys.",
  category: "Forms",
  tags: ["form", "conform", "choice", "selection", "validation"],
}
