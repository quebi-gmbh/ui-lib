import type { ComponentMeta } from "./types"

export const conformFileTriggerMeta: ComponentMeta = {
  slug: "conform-file-trigger",
  name: "Conform File Trigger",
  description:
    "The quebi FileTrigger and DropZone wired to Conform. Binds a file field through a hidden file input, so FormData carries real File objects.",
  category: "Forms",
  tags: ["form", "conform", "file", "upload", "dropzone", "validation"],
}
