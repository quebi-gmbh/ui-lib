import type { ComponentMeta } from "./types"

export const conformMultipleSelectMeta: ComponentMeta = {
  slug: "conform-multiple-select",
  name: "Conform Multiple Select",
  description:
    "Multiple Select wired to Conform: derives name, id, default selection, and validity from field metadata, mirrors the selection into hidden inputs for submission, and renders inline errors. Pass the option list as children.",
  category: "Conform",
  tags: ["conform", "select", "multi-select", "form", "validation", "tags"],
}
