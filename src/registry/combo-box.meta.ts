import type { ComponentMeta } from "./types"

export const comboBoxMeta: ComponentMeta = {
  slug: "combo-box",
  name: "Combo Box",
  description:
    "Autocomplete combo box built on react-aria-components, styled with the quebi design system. Pairs a text input with a filterable dropdown of options, with brand-teal selection and sections. The list opens on focus rather than on the first keystroke; pass menuTrigger=\"input\" or \"manual\" to opt out.",
  category: "Selection",
  tags: ["form", "input", "autocomplete", "select", "dropdown", "interactive"],
}
