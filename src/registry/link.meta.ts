import type { ComponentMeta } from "./types"

export const linkMeta: ComponentMeta = {
  slug: "link",
  name: "Link",
  description:
    "Brand-teal text link built on react-aria-components, styled with the quebi design system. Underlined at rest so it stays distinguishable from body copy; add no-underline for navigation. External hrefs render as a plain anchor.",
  category: "Navigation",
  tags: ["navigation", "link", "anchor", "interactive"],
}
