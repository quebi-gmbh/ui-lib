import type { ComponentMeta } from "./types"

export const monthViewMeta: ComponentMeta = {
  slug: "month-view",
  name: "Month View",
  description:
    "The month as whole weeks, with events as chips that run across the days they cover and fold into \"+N more\" when a cell fills up. Calendar is the month grid you pick a date with; this is the one you read what is on it.",
  category: "Date & time",
  tags: ["calendar", "month", "agenda", "events", "grid", "outlook"],
}
