import type { ComponentMeta } from "./types"

export const asyncMultipleSelectMeta: ComponentMeta = {
  slug: "async-multiple-select",
  name: "Async Multiple Select",
  description:
    "A multi-value picker whose options are loaded from a remote source: the dropdown opens on focus and shows the first page, typing re-queries the source live, and scrolling loads more. Selected options render as removable tags, styled with the quebi dark surface and brand-teal selection.",
  category: "Forms",
  tags: [
    "select",
    "multi-select",
    "async",
    "combobox",
    "tags",
    "form",
    "input",
    "search",
    "pagination",
    "interactive",
  ],
}
