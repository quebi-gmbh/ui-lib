import type { ComponentMeta } from "./types"

export const tabsMeta: ComponentMeta = {
  slug: "tabs",
  name: "Tabs",
  description:
    "Accessible tabs built on react-aria-components, styled with the quebi design system. Quiet inactive tabs with a brand-teal active tab and a 2px indicator on the list rail — an underline when horizontal, a bar on the inline-start edge when vertical.",
  category: "Navigation",
  tags: ["navigation", "tabs", "interactive", "disclosure"],
}
