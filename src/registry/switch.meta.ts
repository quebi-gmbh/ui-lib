import type { ComponentMeta } from "./types"

export const switchMeta: ComponentMeta = {
  slug: "switch",
  name: "Switch",
  description:
    "Accessible toggle switch built on react-aria-components, styled with the quebi design system. The on state fills the track with brand teal, outlined in the brand mark token so the control keeps a boundary on the light page, while the thumb slides across in the inverse surface colour; supports labels, controlled state, and disabled.",
  category: "Selection",
  tags: ["form", "input", "toggle", "boolean", "interactive"],
}
