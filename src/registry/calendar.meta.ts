import type { ComponentMeta } from "./types"

export const calendarMeta: ComponentMeta = {
  slug: "calendar",
  name: "Calendar",
  description:
    "An accessible month calendar built on react-aria-components and @internationalized/date, with a choice of header: one control naming the visible month that opens the Month Picker grid (the default) or chevron steppers via the stepper variant. The selected day fills with brand teal and today is ringed — foundational for Range Calendar and Date Picker.",
  category: "Date & time",
  tags: ["calendar", "date", "datepicker", "form", "input", "interactive"],
}
