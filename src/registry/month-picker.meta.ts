import type { ComponentMeta } from "./types"

export const monthPickerMeta: ComponentMeta = {
  slug: "month-picker",
  name: "Month Picker",
  description:
    "A grid of the twelve months of one year with a year stepper in the header, plus the popover field that wraps it. Month names are locale-formatted through the shared Intl cache, and the value is a CalendarDate on the first of the month.",
  category: "Date & time",
  tags: ["calendar", "date", "month", "picker", "form", "input", "interactive"],
}
