import type { ComponentMeta } from "./types"

export const conformYearPickerMeta: ComponentMeta = {
  slug: "conform-year-picker",
  name: "Conform Year Picker",
  description:
    "The quebi Year Picker wired to Conform. Binds a date field through a hidden input carrying an ISO YYYY-MM-DD string — January 1 of the chosen year — so it validates like every other Date & time field.",
  category: "Conform",
  tags: ["form", "conform", "year", "date", "picker", "validation"],
}
