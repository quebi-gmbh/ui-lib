import type { ComponentMeta } from "./types"

export const scrollAreaMeta: ComponentMeta = {
  slug: "scroll-area",
  name: "Scroll Area",
  description:
    "Scrollable viewport with the quebi native scrollbar — a cyan-tinted pill floating clear of the edges, with no stepper arrows — styled with the quebi design system. Supports vertical, horizontal, or both axes, optional edge fading, and a reserved scrollbar gutter. `className` lands on the scroll container itself, so padding on a ScrollArea insets the content while the scrollbar still hugs the container's edge.",
  category: "Layout",
  tags: ["layout", "scroll", "overflow", "container", "viewport"],
}
