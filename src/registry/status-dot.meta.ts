import type { ComponentMeta } from "./types"

export const statusDotMeta: ComponentMeta = {
  slug: "status-dot",
  name: "Status Dot",
  description:
    "A state carried on something else — an avatar's corner, a table row, an environment name — in five tones with an optional expanding ring for a live connection. The meaning never lives in the colour: its text ships with it, visible or sr-only.",
  category: "Feedback",
  tags: ["status", "presence", "online", "indicator", "dot", "feedback"],
}
