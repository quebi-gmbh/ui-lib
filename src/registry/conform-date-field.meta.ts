import type { ComponentMeta } from "./types"

export const conformDateFieldMeta: ComponentMeta = {
  slug: "conform-date-field",
  name: "Conform Date Field",
  description:
    "The quebi DateField wired to Conform. Binds a date field's name, required, default, and validity from field metadata and renders inline errors.",
  category: "Forms",
  tags: ["form", "conform", "date", "date-field", "validation"],
}
