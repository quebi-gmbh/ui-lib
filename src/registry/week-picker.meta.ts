import type { ComponentMeta } from "./types"

export const weekPickerMeta: ComponentMeta = {
  slug: "week-picker",
  name: "Week Picker",
  description:
    "A month of days where the whole row is the target: hover lights the week, one click selects it, and the value is always a whole week. Weeks start where the locale says, and the gutter numbers them ISO-8601.",
  category: "Date & time",
  tags: ["calendar", "date", "week", "range", "picker", "form", "input", "interactive"],
}
