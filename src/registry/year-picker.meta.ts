import type { ComponentMeta } from "./types"

export const yearPickerMeta: ComponentMeta = {
  slug: "year-picker",
  name: "Year Picker",
  description:
    "A decade grid for picking a year, plus the popover field that wraps it. Pages by decade with the boundary years dimmed in place, honours minValue/maxValue, and yields a CalendarDate on January 1 so it composes with the rest of Date & time.",
  category: "Date & time",
  tags: ["calendar", "date", "year", "picker", "form", "input", "interactive"],
}
