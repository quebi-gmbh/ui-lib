import type { ComponentMeta } from "./types"

export const composedChartMeta: ComponentMeta = {
  slug: "composed-chart",
  name: "Composed Chart",
  description:
    "Bars, lines and areas on one pair of axes, driven by a series list over the quebi Chart wrapper, with an optional second Y axis that appears only when a series asks for it. Requires the recharts package.",
  category: "Charts",
  tags: ["chart", "composed", "combo", "recharts", "dual-axis", "data", "visualization"],
}
